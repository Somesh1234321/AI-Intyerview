# Optional: PostgreSQL connection string used by Drizzle ORM.
#
# This app runs IN-MEMORY by default, so DATABASE_URL is optional.
# Only set it if you want to enable PostgreSQL persistence.
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/app_db

